# Local Docker PostgreSQL for the Azure-focused repository
DATABASE_URL="postgresql://impact_user:impact_password@localhost:5433/impact_logger?schema=public"

# Leave empty locally until Azure Application Insights is configured
APPLICATIONINSIGHTS_CONNECTION_STRING=""